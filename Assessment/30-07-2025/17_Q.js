// Debugging Task: Find the error in this code and fix it:
   let userName;
   console.log("User: " + userName.toUpperCase());

//    username is undefined so we can't use toUpperCase here 

userName = "boobathi";
console.log("User : "+userName.toUpperCase());

